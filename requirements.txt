# SML Base-4 Signal API — minimal production dependencies
# Separate from full SqueezeOS requirements to keep the container lean

fastapi>=0.111
uvicorn[standard]>=0.29
fastmcp>=3.3
aiohttp>=3.9
pandas>=2.2
numpy>=1.26
yfinance>=0.2.36
python-dotenv>=1.0
